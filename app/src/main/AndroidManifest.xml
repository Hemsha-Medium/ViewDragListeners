<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hemsha.exploring.dragandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExploringDragAndroid">

        <activity android:name=".activities.DragToDelete"
            android:screenOrientation="portrait"/>

        <activity android:name=".activities.DragViewWithThumb"
            android:screenOrientation="portrait"/>

        <activity android:name=".activities.DragShadow"
            android:screenOrientation="portrait"/>

        <activity android:name=".activities.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>